/*
 * Optional: the firmware builds and provisions Wi-Fi on-device with no
 * secrets.h at all. Copy this file to `secrets.h` (which is git-ignored) and
 * fill in your values only if you want to pre-seed Wi-Fi creds during
 * development and skip the on-device provisioning flow.
 */
#pragma once

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASSWORD   "your-wifi-password"
