/*
 * Copy this file to `secrets.h` (which is git-ignored) and fill in your values.
 * secrets.h holds Wi-Fi credentials and, later, the Orion device serial.
 */
#pragma once

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASSWORD   "your-wifi-password"

/* Orion Sleep device serial number (from the Orion app / list_devices).
 * Used later to target set_zone / get_device_state. */
#define ORION_SERIAL    ""
